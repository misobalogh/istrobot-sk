<x-guest-layout>
    <div>
        <h1 class="mt-6 text-xl font-semibold text-gray-900 dark:text-white">Categories for the Year {{ $year }}</h1>
        @include('main.categories')
    </div>
    <div>
        <h1 class="mt-6 text-xl font-semibold text-gray-900 dark:text-white">Categories for the Year {{ $year }}</h1>
        @include('main.categories')
    </div>
</x-guest-layout>